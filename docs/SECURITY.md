# Security

Amka runs locally as a normal user.

V0 has no telemetry, network access, persistence, shell execution, subprocess execution, or arbitrary command execution.

Please report active vulnerabilities privately rather than through a public issue. This repository does not currently publish a dedicated security contact address.
