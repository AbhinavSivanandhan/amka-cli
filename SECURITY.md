# Security

Amka runs locally as a normal user.

The current version has no telemetry, network access, or arbitrary shell execution.

Please report active vulnerabilities privately rather than through public issues. This repository does not currently publish a dedicated security contact address.
